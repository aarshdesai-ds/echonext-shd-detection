# HF Space deps. Gradio itself is installed by the Space SDK (latest 5.x, since
# README has no sdk_version pin) — keeping it out of here avoids version clashes.
git+https://github.com/aarshdesai-ds/echonext-shd-detection.git
tensorflow-cpu==2.18.0
matplotlib==3.*
huggingface_hub>=0.24
numpy<2.0
