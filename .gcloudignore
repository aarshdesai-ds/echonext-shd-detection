# Cloud Build upload context. IMPORTANT: this file overrides .gitignore for
# `gcloud builds submit`, so models/ (gitignored) IS uploaded and bakeable.
.git
.github
.venv
venv
__pycache__
.pytest_cache
.ruff_cache
notebooks
tests
*.md
!model_card.md
# models/ is intentionally NOT listed here — it must reach the build context.
