# Extra deps for the combined API+demo serving image (on top of requirements.txt).
# Gradio 5 (not 4.x) to avoid the HfFolder / schema / Starlette conflicts that
# 4.44 hits against a current dependency stack.
-r requirements.txt
gradio>=5,<6
matplotlib==3.*
